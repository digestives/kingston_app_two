<h1>Editing user</h1>
  <%= form_for(@user) do |f| %>
    <%= render 'edit', :f => f %>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>

