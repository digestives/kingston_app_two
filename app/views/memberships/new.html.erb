<div class="span-12">

<h1>New membership</h1>

<%= form_for(@membership) do |f| %>

  <%= render 'form', :f => f %>
  <div class="actions">
    <%= f.submit :remote => true %>
  </div>

<% end %>

<%= link_to 'Back', memberships_path %>

</div>

