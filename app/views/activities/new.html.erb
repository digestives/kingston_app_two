<div class="span-12">

  <h1>New activity</h1>

  <%= form_for(@activity) do |f| %>
  <%= render 'form', :f => f %>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>

  <%= link_to 'Back', activities_path %>

</div>

